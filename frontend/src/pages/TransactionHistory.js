import React from 'react';

const TransactionHistory = () => (
  <div className="page">
    <h2>Transaction History</h2>
    <p>Transaction history view (placeholder).</p>
  </div>
);

export default TransactionHistory;
